# RAG service dependencies.
# IMPORTANT: install in a Python 3.11 venv — torch / sentence-transformers /
# chromadb wheels are most reliable there.
#
#   py -3.11 -m venv .rag-venv
#   .rag-venv\Scripts\activate
#   pip install -r rag/requirements.txt
#   uvicorn rag_server:app --port 8100
#
# On Render set PYTHON_VERSION=3.11.9 (see render.yaml).

# Pull the CPU-only torch build — the default PyPI wheel bundles CUDA (~800MB)
# and is pointless on Render's CPU instances.
--extra-index-url https://download.pytorch.org/whl/cpu
torch

pymupdf
ebooklib
beautifulsoup4
chromadb
sentence-transformers
# Code uses `from google import genai` -> this is the google-genai SDK,
# NOT the older google-generativeai package.
google-genai
fastapi
uvicorn[standard]
pydantic
python-dotenv
